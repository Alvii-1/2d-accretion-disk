/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./shaders/body.wgsl"
/*!***************************!*\
  !*** ./shaders/body.wgsl ***!
  \***************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__)\n/* harmony export */ });\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (\"// struct for celestial celestial bodies matching in types.ts\\nstruct Body \\n{\\n    position: vec2f,\\n    velocity: vec2f,\\n    mass: f32,\\n    radius: f32,\\n    alive: u32,\\n    padding: u32,\\n    color: vec4f,\\n}\\n\\n// returned struct object from vertexMain\\nstruct VertexOutput \\n{\\n    @builtin(position) position : vec4f,\\n    @location(0) localPos: vec2f, // for circles clipping\\n    @location(1) color : vec4f,\\n}\\n\\n// stored number of bodies + their characteristics\\n@group(0) @binding(0) var<storage, read> bodies: array<Body>;\\n@group(0) @binding(1) var<uniform> zoom: f32;\\n\\n@vertex // vertex shader\\nfn vertexMain(@builtin(vertex_index) vIdx : u32, @builtin(instance_index) iIdx : u32) -> VertexOutput \\n{\\n\\n    // draw a square first then cut it to make the circle\\n    // first draw the square\\n    var square = array<vec2f, 6>(\\n        vec2f(-1, -1), \\n        vec2f(1, -1), \\n        vec2f(-1, 1),\\n        vec2f(-1, 1), \\n        vec2f(1, -1), \\n        vec2f(1, 1)\\n    );\\n\\n    let body = bodies[iIdx];   // current body\\n    let localPos = square[vIdx]; // current quad vertex pos\\n\\n    // then scale the square by radius to get the circle\\n    let worldPos = body.position + localPos * body.radius;\\n\\n    // convert the pixels to clip space, place the square on screen\\n    // also incorporate the zoom\\n    let center = vec2f(600.0, 400.0);\\n    let zoomed = (worldPos - center) / zoom + center;\\n    let clipX = (zoomed.x / 1200.0) * 2.0 - 1.0;\\n    let clipY = 1.0 - (zoomed.y / 800.0) * 2.0;\\n\\n    // setup vertex output \\n    var output: VertexOutput;\\n\\n    // if the body is dead move the vertex off screen so it doesnt render\\n    if (body.alive ==0u) {\\n        output.position = vec4f(-10.0, -10.0, 0.0, 1.0);\\n        return output;\\n    }\\n\\n    // keep writing the rest of the output for alive bodies\\n    output.position = vec4f(clipX, clipY, 0.0, 1.0);\\n    output.localPos = localPos;\\n    output.color = body.color;\\n    return output;\\n}\\n\\n@fragment // fragment shader\\nfn fragmentMain(input: VertexOutput) -> @location(0) vec4f\\n{\\n    if (length(input.localPos) > 1.0) { discard; }\\n    return input.color;\\n}\");\n\n//# sourceURL=webpack://webgpu-hello/./shaders/body.wgsl?\n}");

/***/ },

/***/ "./shaders/physics.wgsl"
/*!******************************!*\
  !*** ./shaders/physics.wgsl ***!
  \******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__)\n/* harmony export */ });\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (\"// struct for celestial celestial bodies matching in types.ts\\nstruct Body \\n{\\n    position: vec2f,\\n    velocity: vec2f,\\n    mass: f32,\\n    radius: f32,\\n    alive: u32,\\n    padding: u32,\\n    color: vec4f,\\n}\\n\\n// struct for simulation parameters\\nstruct SimParams\\n{\\n    G: f32,             // grav constant\\n    dt: f32,            // time step\\n    bodyCount: u32,     // num bodies in sim\\n    centralMass: f32,   // central mass (anchor point)\\n}\\n\\n@group(0) @binding(0) var<storage, read> bodiesIn: array<Body>;         // bodies initially in the sim\\n@group(0) @binding(1) var<storage, read_write> bodiesOut: array<Body>;  // bodies exiting the sim (some will die)\\n@group(0) @binding(2) var<uniform> params: SimParams;                   // simulation information\\n\\n// compute shader to handle physics calculations between bodies. each thread handles a body\\n@compute @workgroup_size(64)\\nfn computeMain( @builtin(global_invocation_id) id: vec3u ) \\n{\\n    if (id.x >= params.bodyCount) { return; } // leave if thread is out of bounds\\n\\n    let currBody = bodiesIn[id.x];\\n\\n    if (id.x == 0u) { bodiesOut[id.x] = currBody; return; }            // we dont change central mass \\n    if (currBody.alive == 0u) { bodiesOut[id.x] = currBody; return; }  // if body is dead skip changing it\\n\\n    // set up init accelerations (0)\\n    var ax: f32 = 0.0;\\n    var ay: f32 = 0.0;\\n\\n    for (var j: u32 = 0u; j < params.bodyCount; j++) {\\n        if (j == id.x) { continue; }               // skip self\\n        if (bodiesIn[j].alive == 0u) { continue; } // skip dead bodies too\\n        \\n        // get other body then get difference in position in x and y\\n        let other = bodiesIn[j];\\n        let dx = other.position.x - bodiesIn[id.x].position.x;\\n        let dy = other.position.y - bodiesIn[id.x].position.y;\\n\\n        // get the difference in pos diagonal\\n        let r = sqrt(dx*dx + dy*dy + 100.0);  // epsilon squared = 100\\n\\n        // compute acceleration change and apply it\\n        let acc = params.G * other.mass / (r * r * r);\\n        ax += acc * dx;\\n        ay += acc * dy;\\n    }   \\n\\n    // updated position and velocity resulting from calc\\n    var updated = currBody;\\n    updated.position = currBody.position + currBody.velocity * params.dt;\\n    updated.velocity = currBody.velocity + vec2f(ax, ay) * params.dt;\\n    bodiesOut[id.x] = updated;\\n}\\n\");\n\n//# sourceURL=webpack://webgpu-hello/./shaders/physics.wgsl?\n}");

/***/ },

/***/ "./src/main.ts"
/*!*********************!*\
  !*** ./src/main.ts ***!
  \*********************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _renderer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./renderer */ \"./src/renderer.ts\");\n\nfunction generateBodies(count, massVariance, minDist, maxDist) {\n    // setup empty array\n    let bodies = [];\n    // first thing is the main body (sun)\n    bodies.push({\n        x: 600, y: 400,\n        vx: 0, vy: 0,\n        mass: 1e10,\n        radius: 200,\n        alive: 1,\n        color: [1, 0.8, 0, 1]\n    });\n    // need gravitational constant and fixed vals before populating\n    const G = 6.674e-3; // enlarged from -11\n    const cx = 600, cy = 400; // center known\n    const baseMass = 1e6;\n    // then populate the rest randomly\n    for (let i = 1; i < count; i++) {\n        const angle = Math.random() * Math.PI * 2;\n        const dist = minDist + Math.random() * (maxDist - minDist);\n        const mass = baseMass + Math.random() * massVariance;\n        const radius = 3 + (mass / baseMass);\n        // position on circle\n        const x = cx + dist * Math.cos(angle);\n        const y = cy + dist * Math.sin(angle);\n        // circular orbit velocity perpendicular to radius\n        const v = Math.sqrt(G * 1e10 / dist);\n        const vx = -v * Math.sin(angle);\n        const vy = v * Math.cos(angle);\n        bodies.push({\n            x, y, vx, vy,\n            mass, radius,\n            alive: 1,\n            color: [0.1 + Math.random() * 0.5, 0.1 + Math.random() * 0.5, 0.1 + Math.random() * 0.5, 1]\n        });\n    }\n    return bodies;\n}\n// check collisions with the sun and other bodies. if the sun is overlapping\n// pos, then eat the body. if the body hits another body, grow in size\nfunction checkCollisions(bodies, device, buffer) {\n    // hold set of changed bodies so we dont reiterate over unchanged ones\n    const changed = new Set();\n    for (let i = 1; i < bodies.length; i++) {\n        if (bodies[i].alive === 0)\n            continue;\n        // check against central body\n        const dx0 = bodies[i].x - bodies[0].x;\n        const dy0 = bodies[i].y - bodies[0].y;\n        const dist0 = Math.sqrt(dx0 * dx0 + dy0 * dy0);\n        if (dist0 <= bodies[0].radius * 1.15 + bodies[i].radius) {\n            bodies[i].alive = 0;\n            changed.add(i);\n            continue;\n        }\n        // check against all other small bodies\n        for (let j = i + 1; j < bodies.length; j++) {\n            if (bodies[i].mass > 1e8 || bodies[j].mass > 1e8)\n                continue; // if we're the big masses we're immortal\n            if (bodies[j].alive === 0)\n                continue;\n            const dx = bodies[j].x - bodies[i].x;\n            const dy = bodies[j].y - bodies[i].y;\n            const dist = Math.sqrt(dx * dx + dy * dy);\n            if (dist < bodies[i].radius + bodies[j].radius) {\n                const totalMass = bodies[i].mass + bodies[j].mass;\n                bodies[i].vx = (bodies[i].vx * bodies[i].mass + bodies[j].vx * bodies[j].mass) / totalMass;\n                bodies[i].vy = (bodies[i].vy * bodies[i].mass + bodies[j].vy * bodies[j].mass) / totalMass;\n                bodies[i].mass = totalMass;\n                bodies[i].radius = Math.sqrt(bodies[i].radius ** 2 + bodies[j].radius ** 2 * 0.75);\n                bodies[j].alive = 0;\n                changed.add(i);\n                changed.add(j);\n            }\n        }\n    }\n    // write changed bodies back to GPU buffer\n    for (const idx of changed) {\n        const offset = idx * 12 * 4;\n        const data = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.bodiesToFloat32Array)([bodies[idx]]);\n        device.queue.writeBuffer(buffer, offset, data.buffer);\n    }\n}\nasync function main() {\n    // setup the canvas based on the HTML element that has matching ID\n    const canvas = document.getElementById('webgpu-canvas');\n    if (!canvas) {\n        throw new Error('HTML Canvas not found');\n    }\n    // setup the device, context, and format from render.ts initializeWebGPU() function\n    const { device, context, format } = await (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.initializeWebGPU)(canvas);\n    let bodies = generateBodies(500, 1e6, 80, 300);\n    // setup the sim by calling the func above to get the bodies\n    //const bodies = generateBodies(1000, 1e6,);\n    const state = {\n        bodies,\n        dt: 0.008\n    };\n    // setup render pipeline for drawing bodies\n    const { pipeline: renderPipeline, bindGroupLayout: renderBGL } = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createRenderPipeline)(device, format);\n    // setup compute pipeline for physics\n    const { pipeline: computePipeline, bindGroupLayout: computeBGL } = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createComputePipeline)(device);\n    // buffers for data which require overwriting \n    // since the sim state changes (some things get unalive )\n    let bufferA = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createGPUBuffer)(device, state.bodies);\n    let bufferB = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createGPUBuffer)(device, state.bodies);\n    // params buffer is needed in the compute shader at each thread, needs\n    // to know things like grav constant and bodieslength \n    const paramsBuffer = device.createBuffer({\n        size: 16,\n        usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST\n    });\n    const paramsData = new ArrayBuffer(16);\n    new Float32Array(paramsData, 0, 2).set([6.674e-3, state.dt]);\n    new Uint32Array(paramsData, 8, 1).set([state.bodies.length]);\n    new Float32Array(paramsData, 12, 1).set([1e10]);\n    device.queue.writeBuffer(paramsBuffer, 0, paramsData);\n    // for zooming out \n    const zoomBuffer = device.createBuffer({\n        size: 4, // single f32\n        usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST\n    });\n    device.queue.writeBuffer(zoomBuffer, 0, new Float32Array([1.0]));\n    // setup bind groups for compute and render\n    let computeBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeComputeBindGroup)(device, computeBGL, bufferA, bufferB, paramsBuffer);\n    let renderBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeBindGroup)(device, renderBGL, bufferB, zoomBuffer);\n    // Setup HTML elements functionality\n    // get slider elements first\n    const bodyCountSlider = document.getElementById('bodyCount');\n    const massVarianceSlider = document.getElementById('massVariance');\n    const minRadiusSlider = document.getElementById('minRadius');\n    const maxRadiusSlider = document.getElementById('maxRadius');\n    const zoomSlider = document.getElementById('zoom');\n    // get display spans\n    const countVal = document.getElementById('countVal');\n    const varianceVal = document.getElementById('varianceVal');\n    const minRadVal = document.getElementById('minRadVal');\n    const maxRadVal = document.getElementById('maxRadVal');\n    const zoomVal = document.getElementById('zoomVal');\n    // update display values when sliders move\n    bodyCountSlider.addEventListener('input', () => countVal.textContent = bodyCountSlider.value);\n    massVarianceSlider.addEventListener('input', () => varianceVal.textContent = massVarianceSlider.value);\n    minRadiusSlider.addEventListener('input', () => minRadVal.textContent = minRadiusSlider.value);\n    maxRadiusSlider.addEventListener('input', () => maxRadVal.textContent = maxRadiusSlider.value);\n    // inserting second mass\n    let insertMode = false;\n    const insertBtn = document.getElementById('insertBtn');\n    const secondaryMassInput = document.getElementById('secondaryMass');\n    insertBtn.addEventListener('click', () => {\n        insertMode = !insertMode;\n        insertBtn.textContent = insertMode ? 'Click Canvas to Place' : 'Insert Mass Mode';\n        insertBtn.style.background = insertMode ? '#ff4444' : '#ffcc00';\n    });\n    canvas.addEventListener('click', (e) => {\n        if (!insertMode)\n            return;\n        const cx = 600, cy = 400;\n        const zoom = parseFloat(zoomSlider.value);\n        const x = (e.offsetX - cx) * zoom + cx;\n        const y = (e.offsetY - cy) * zoom + cy;\n        const mass = parseFloat(secondaryMassInput.value);\n        // distance from central body\n        const dx = x - cx;\n        const dy = y - cy;\n        const dist = Math.sqrt(dx * dx + dy * dy);\n        const angle = Math.atan2(dy, dx);\n        // stable circular orbit velocity\n        const G = 6.674e-3;\n        const v = Math.sqrt(G * 1e10 / dist);\n        const vx = -v * Math.sin(angle);\n        const vy = v * Math.cos(angle);\n        state.bodies.push({\n            x, y, vx, vy,\n            mass,\n            radius: 40,\n            alive: 1,\n            color: [1, 0.3, 0.3, 1] // red\n        });\n        // recreate buffers to fit new body\n        bufferA = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createGPUBuffer)(device, state.bodies);\n        bufferB = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createGPUBuffer)(device, state.bodies);\n        computeBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeComputeBindGroup)(device, computeBGL, bufferA, bufferB, paramsBuffer);\n        renderBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeBindGroup)(device, renderBGL, bufferB, zoomBuffer);\n        // update params with new body count\n        const newParamsData = new ArrayBuffer(16);\n        new Float32Array(newParamsData, 0, 2).set([6.674e-3, state.dt]);\n        new Uint32Array(newParamsData, 8, 1).set([state.bodies.length]);\n        new Float32Array(newParamsData, 12, 1).set([1e10]);\n        device.queue.writeBuffer(paramsBuffer, 0, newParamsData);\n        insertMode = false;\n        insertBtn.textContent = 'Insert Mass Mode';\n        insertBtn.style.background = '#ffcc00';\n    });\n    // speed multiplier\n    const speedSlider = document.getElementById('speedMultiplier');\n    const speedVal = document.getElementById('speedVal');\n    speedSlider.addEventListener('input', () => speedVal.textContent = speedSlider.value);\n    zoomSlider.addEventListener('input', () => {\n        zoomVal.textContent = zoomSlider.value,\n            device.queue.writeBuffer(zoomBuffer, 0, new Float32Array([parseFloat(zoomSlider.value)]));\n    });\n    document.getElementById('generateBtn').addEventListener('click', () => {\n        const count = parseInt(bodyCountSlider.value);\n        const variance = parseFloat(massVarianceSlider.value) * 1e5;\n        const minDist = parseFloat(minRadiusSlider.value);\n        const maxDist = parseFloat(maxRadiusSlider.value);\n        state.bodies = generateBodies(count, variance, minDist, maxDist);\n        bufferA = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createGPUBuffer)(device, state.bodies);\n        bufferB = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.createGPUBuffer)(device, state.bodies);\n        computeBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeComputeBindGroup)(device, computeBGL, bufferA, bufferB, paramsBuffer);\n        renderBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeBindGroup)(device, renderBGL, bufferB, zoomBuffer);\n        // update body count in params\n        const newParamsData = new ArrayBuffer(16);\n        new Float32Array(newParamsData, 0, 2).set([6.674e-3, state.dt]);\n        new Uint32Array(newParamsData, 8, 1).set([state.bodies.length]);\n        new Float32Array(newParamsData, 12, 1).set([1e10]);\n        device.queue.writeBuffer(paramsBuffer, 0, newParamsData);\n    });\n    let frameCount = 0;\n    // render loop. sets up the command encoder dispatch calls and swaps the buffers as needed, then renders\n    function loop() {\n        const speed = parseInt(speedSlider.value);\n        for (let s = 0; s < speed; s++) {\n            const encoder = device.createCommandEncoder();\n            const computePass = encoder.beginComputePass();\n            computePass.setPipeline(computePipeline);\n            computePass.setBindGroup(0, computeBindGroup);\n            computePass.dispatchWorkgroups(Math.ceil(state.bodies.length / 64));\n            computePass.end();\n            device.queue.submit([encoder.finish()]);\n            [bufferA, bufferB] = [bufferB, bufferA];\n            computeBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeComputeBindGroup)(device, computeBGL, bufferA, bufferB, paramsBuffer);\n            renderBindGroup = (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.makeBindGroup)(device, renderBGL, bufferB, zoomBuffer);\n        }\n        // // compute pass\n        // const encoder = device.createCommandEncoder();\n        // const computePass = encoder.beginComputePass();\n        // computePass.setPipeline(computePipeline);\n        // computePass.setBindGroup(0, computeBindGroup);\n        // computePass.dispatchWorkgroups(Math.ceil(state.bodies.length / 64));\n        // computePass.end();\n        // device.queue.submit([encoder.finish()]);\n        // // swap buffers\n        // [bufferA, bufferB] = [bufferB, bufferA];\n        // computeBindGroup = makeComputeBindGroup(device, computeBGL, bufferA, bufferB, paramsBuffer);\n        // renderBindGroup = makeBindGroup(device, renderBGL, bufferB, zoomBuffer);\n        // render\n        (0,_renderer__WEBPACK_IMPORTED_MODULE_0__.renderPass)(device, context, renderPipeline, renderBindGroup, state.bodies.length);\n        frameCount++;\n        if (frameCount % 5 === 0) {\n            // read current body data from GPU back to CPU\n            const readBuffer = device.createBuffer({\n                size: state.bodies.length * 12 * 4,\n                usage: GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ\n            });\n            const encoder = device.createCommandEncoder();\n            encoder.copyBufferToBuffer(bufferB, 0, readBuffer, 0, state.bodies.length * 12 * 4);\n            device.queue.submit([encoder.finish()]);\n            readBuffer.mapAsync(GPUMapMode.READ).then(() => {\n                const data = new Float32Array(readBuffer.getMappedRange());\n                // update state.bodies positions from GPU data\n                for (let i = 0; i < state.bodies.length; i++) {\n                    state.bodies[i].x = data[i * 12 + 0];\n                    state.bodies[i].y = data[i * 12 + 1];\n                    state.bodies[i].vx = data[i * 12 + 2];\n                    state.bodies[i].vy = data[i * 12 + 3];\n                }\n                readBuffer.unmap();\n                readBuffer.destroy();\n                checkCollisions(state.bodies, device, bufferA);\n                checkCollisions(state.bodies, device, bufferB);\n            });\n        }\n        requestAnimationFrame(loop);\n    }\n    requestAnimationFrame(loop);\n}\ndocument.addEventListener('DOMContentLoaded', main);\n\n\n//# sourceURL=webpack://webgpu-hello/./src/main.ts?\n}");

/***/ },

/***/ "./src/renderer.ts"
/*!*************************!*\
  !*** ./src/renderer.ts ***!
  \*************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   bodiesToFloat32Array: () => (/* binding */ bodiesToFloat32Array),\n/* harmony export */   createComputePipeline: () => (/* binding */ createComputePipeline),\n/* harmony export */   createGPUBuffer: () => (/* binding */ createGPUBuffer),\n/* harmony export */   createRenderPipeline: () => (/* binding */ createRenderPipeline),\n/* harmony export */   initializeWebGPU: () => (/* binding */ initializeWebGPU),\n/* harmony export */   makeBindGroup: () => (/* binding */ makeBindGroup),\n/* harmony export */   makeComputeBindGroup: () => (/* binding */ makeComputeBindGroup),\n/* harmony export */   renderPass: () => (/* binding */ renderPass)\n/* harmony export */ });\n/* harmony import */ var _shaders_body_wgsl__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shaders/body.wgsl */ \"./shaders/body.wgsl\");\n/* harmony import */ var _shaders_physics_wgsl__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shaders/physics.wgsl */ \"./shaders/physics.wgsl\");\n\n\n// Doing typical WebGPU Setup here, just getting it out of main\nasync function initializeWebGPU(canvas) {\n    // get adapter and device\n    const adapter = await navigator.gpu.requestAdapter();\n    if (!adapter) {\n        throw new Error('No GPU adapter found');\n    }\n    const device = await adapter.requestDevice();\n    console.log(adapter); // DEBUG\n    console.log(device); // DEBUG\n    // setup context and format\n    const context = canvas.getContext(\"webgpu\");\n    const format = navigator.gpu.getPreferredCanvasFormat();\n    if (!context) {\n        throw new Error('Could not get WebGPU context');\n    }\n    // return the device, context, and format\n    context.configure({\n        device: device,\n        format: format\n    });\n    return { device, context, format };\n}\n// This function will setup the render pipeline and bind group layout,\n// it will also return both created render pipeline and bg layout\nfunction createRenderPipeline(device, format) {\n    // Setup where the shader code is \n    const shaderModule = device.createShaderModule({ code: _shaders_body_wgsl__WEBPACK_IMPORTED_MODULE_0__[\"default\"] });\n    // Specify the read-only bind group layout which is only visible\n    // to the vertex shader and will contain the bodies eventually\n    const bindGroupLayout = device.createBindGroupLayout({\n        entries: [{\n                binding: 0,\n                visibility: GPUShaderStage.VERTEX,\n                buffer: { type: 'read-only-storage' }\n            }, {\n                binding: 1,\n                visibility: GPUShaderStage.VERTEX,\n                buffer: { type: 'uniform' }\n            }]\n    });\n    // setup render pipeline, match shader entry points and specify\n    // the created bindgrouplayout \n    const renderPipeline = device.createRenderPipeline({\n        vertex: {\n            module: shaderModule,\n            entryPoint: \"vertexMain\"\n        },\n        fragment: {\n            module: shaderModule,\n            entryPoint: \"fragmentMain\",\n            targets: [{ format }]\n        },\n        layout: device.createPipelineLayout({\n            bindGroupLayouts: [bindGroupLayout]\n        })\n    });\n    return {\n        pipeline: renderPipeline,\n        bindGroupLayout: bindGroupLayout\n    };\n}\n// similar as above, but for the compute shader in physics.wgsl\nfunction createComputePipeline(device) {\n    // Setup where the shader code is \n    const computeModule = device.createShaderModule({ code: _shaders_physics_wgsl__WEBPACK_IMPORTED_MODULE_1__[\"default\"] });\n    // Specify the read-only bind group layout which is only visible\n    // to the compute shader and will contain the bodies eventually\n    // this time we have 3 bindings \n    const bindGroupLayout = device.createBindGroupLayout({\n        entries: [{\n                binding: 0,\n                visibility: GPUShaderStage.COMPUTE,\n                buffer: { type: 'read-only-storage' }\n            }, {\n                binding: 1,\n                visibility: GPUShaderStage.COMPUTE,\n                buffer: { type: 'storage' }\n            }, {\n                binding: 2,\n                visibility: GPUShaderStage.COMPUTE,\n                buffer: { type: 'uniform' }\n            }]\n    });\n    // setup render pipeline, match shader entry points and specify\n    // the created bindgrouplayout \n    const computePipeline = device.createComputePipeline({\n        compute: {\n            module: computeModule,\n            entryPoint: \"computeMain\"\n        },\n        layout: device.createPipelineLayout({\n            bindGroupLayouts: [bindGroupLayout]\n        })\n    });\n    return {\n        pipeline: computePipeline,\n        bindGroupLayout: bindGroupLayout\n    };\n}\n// this function will map the buffer indexes to the float32 array positions\nfunction bodiesToFloat32Array(bodies) {\n    const data = new Float32Array(bodies.length * 12);\n    for (let i = 0; i < bodies.length; i++) {\n        const body = bodies[i];\n        data[i * 12 + 0] = body.x;\n        data[i * 12 + 1] = body.y;\n        data[i * 12 + 2] = body.vx;\n        data[i * 12 + 3] = body.vy;\n        data[i * 12 + 4] = body.mass;\n        data[i * 12 + 5] = body.radius;\n        data[i * 12 + 6] = body.alive;\n        data[i * 12 + 7] = 0; // padding\n        data[i * 12 + 8] = body.color[0];\n        data[i * 12 + 9] = body.color[1];\n        data[i * 12 + 10] = body.color[2];\n        data[i * 12 + 11] = body.color[3];\n    }\n    return data;\n}\n// Here we create the GPU storage buffer that the shader will end up\n// reading the celestial body data from\nfunction createGPUBuffer(device, bodies) {\n    // fill the array of instances of bodies from the function above,\n    // then make the GPU buffer. Storage, COPY DST and COPY SRC because\n    // \n    let data = bodiesToFloat32Array(bodies);\n    let gpuBuffer = device.createBuffer({\n        size: data.byteLength,\n        usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST | GPUBufferUsage.COPY_SRC,\n    });\n    // write the buffer and return it\n    device.queue.writeBuffer(gpuBuffer, 0, data.buffer);\n    return gpuBuffer;\n}\n// now we need to create the bind group itself, which is done in this function\n// This is specifically for the drawing, not the compute side\nfunction makeBindGroup(device, bindGroupLayout, bodiesBuffer, zoomBuffer) {\n    // setup the bind group to use the correct layout and assign its binding\n    // and entries. it will hold the data on the celestial bodies\n    return device.createBindGroup({\n        layout: bindGroupLayout,\n        entries: [{\n                binding: 0,\n                resource: { buffer: bodiesBuffer }\n            }, {\n                binding: 1,\n                resource: { buffer: zoomBuffer }\n            }]\n    });\n}\n// Now we also need a bind group for the compute shader,\n// which needs the buffer for the bodies and the simulation params\nfunction makeComputeBindGroup(device, bindGroupLayout, bodiesIn, bodiesOut, paramsBuffer) {\n    // same as above but for the physics side, which has 3 bindings\n    return device.createBindGroup({\n        layout: bindGroupLayout,\n        entries: [\n            { binding: 0, resource: { buffer: bodiesIn } },\n            { binding: 1, resource: { buffer: bodiesOut } },\n            { binding: 2, resource: { buffer: paramsBuffer } }\n        ]\n    });\n}\n// The entire render pass with the command encoder batched\n// instructions setup\nfunction renderPass(device, context, pipeline, bindGroup, bodyCount) {\n    // setup command encoder\n    const commandEncoder = device.createCommandEncoder();\n    // setup render pass\n    const renderPass = commandEncoder.beginRenderPass({\n        colorAttachments: [{\n                view: context.getCurrentTexture().createView(),\n                clearValue: { r: 0, g: 0, b: 0, a: 1 },\n                loadOp: \"clear\",\n                storeOp: \"store\"\n            }]\n    });\n    // call render pass functions and complete the command encoder command\n    // sequence \n    renderPass.setPipeline(pipeline);\n    renderPass.setBindGroup(0, bindGroup);\n    renderPass.draw(6, bodyCount);\n    renderPass.end();\n    device.queue.submit([commandEncoder.finish()]);\n}\n\n\n//# sourceURL=webpack://webgpu-hello/./src/renderer.ts?\n}");

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		if (!(moduleId in __webpack_modules__)) {
/******/ 			delete __webpack_module_cache__[moduleId];
/******/ 			var e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = __webpack_require__("./src/main.ts");
/******/ 	
/******/ })()
;